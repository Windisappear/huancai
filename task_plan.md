# 幻彩静态版实施计划

目标：在本目录完成纯前端生图网站，沿用 D:/生图网站 的界面设计，直连多元探索 API，仅接入 GPT Image / Nano Banana / Seedream，支持文生图、图生图、本地历史。

- [x] 阅读原项目 UI 与官方接口文档。
- [x] 实现静态页面、模型适配、配置、IndexedDB 图库。
- [x] 验证请求参数、错误/取消/部分完成、本地持久化、桌面与移动端。
- [x] 交付启动方式和静态部署文件。

约束：不改原项目，不复制密钥，不增加后端；启动脚本仅提供静态文件。真实收费调用需用户提供此项目使用的 Key；本次用拦截响应验证完整流程。

问题：文档概览一次获取超时，改为读取各模型独立接口页面已解决。UI 设计检索返回叙事型样式，与工作台不适配，继续以用户指定原设计为准。

当前托管要求：用户澄清 Vercel 只是举例，目标为 PocketBay，不需要 Vercel 配置。已删除此前新增的 vercel.json、.vercelignore，改为通用 dist 与根目录 index.html ZIP。已询问可选平台文档链接；不影响交付静态包。

用户提供 https://pocketbay.com/deploy，官方搜索结果确认原生 static + ZIP 上传，已据此更新说明；没有创建部署会话或上传用户项目。

验证记录：8 项接口测试通过；初次浏览器测试断言等待在异步持久化之前结束，已修正测试等待条件（新任务出现且生成按钮恢复）。系统 PowerShell 对中转站 OPTIONS 的 TLS 握手失败，未据此声称已验证真实 CORS。
