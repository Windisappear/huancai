# AI 图片生成平台需求与实施方案 v0.2

## 1. 产品范围

面向国内用户的商业 AI 创作网站，云服务器自托管，中文专业工作台。提供文生图与图生图，账号密码登录，人民币充值余额和按张计费。

| 项目 | 要求 |
| --- | --- |
| 模型 | GPT、Gemini、Grok、Seedream；具体型号以实际接口验证为准 |
| 接入 | 首期 https://api.laoliimage2.win/ 中转，后补文档；官方接口独立预留 |
| 尺寸 | 只显示当前模型原生尺寸、质量、张数与参考图能力 |
| 图生图 | 参考图＋文字指令，不默认包括蒙版、局部重绘或图片编辑器 |
| 登录 | 用户名密码；手机、邮箱字段预留，验证码暂不开启 |
| 付费 | 人民币余额；单张价格与本次总价透明显示，不采用积分 |
| 历史 | 账号下跨设备同步，作品与提示词最多保留 72 小时 |
| 后续 | Seedance、Grok、MiniMax、Veo 视频生成 |
| 排除 | New API、SD、MJ、社区、订阅会员、首版视频编辑器 |

真实 API、支付商户、域名和服务器账户、正式售价后补。未验证能力不可销售；测试模式必须明确标识，不把测试图案当作 AI 生成。

## 2. 页面与交互

暂定名称“幻彩”。石墨深色、清晰排版、大幅结果图片、克制强调色；使用 frontend-design 与 UI UX Pro Max 技能辅助设计，不采用聊天气泡布局。

- 创作工作台：模型、提示词、原生尺寸、质量、张数；图生图显示拖放上传、缩略图、移除与替换。
- 生成按钮附近显示单价、数量、总价；账户区域显示可用余额，钱包区显示处理中金额。
- 结果区支持状态展示、预览、原图下载与复用参数。
- 历史页按时间展示提示词、参考图、结果与到期时间。
- 钱包提供充值订单、金额流水和退款申请。
- 账户提供修改密码、恢复码、退出当前及其他设备。
- 管理台提供模型／渠道开关、价格版本、退款审核、异常任务核查。
- 手机采用上下布局，核心流程可操作，无横向溢出；键盘焦点明确，尊重减少动态效果偏好。

## 3. 账号和安全

用户名唯一，密码使用 Argon2id 散列，服务端会话配合 HttpOnly、SameSite Cookie，生产必须使用 HTTPS/Secure。

注册时一次性展示恢复码，仅保存其散列；恢复密码消耗旧码、签发新码并撤销全部会话。修改密码也更换恢复码。首版未验证的电话或邮箱不能找回账户。

注册、登录、恢复及上传限流。管理员由受控服务器命令授权，不能自行注册管理员角色。用户的报价、任务、图片和订单必须按归属校验。管理操作留审计记录。密钥只保存在服务端环境中，不能下发前端或出现在普通日志。

## 4. 人民币计费与支付

推荐人民币余额：价格直观，无需换算；后续促销可引入独立优惠规则。

1. 服务端按模型／模式／尺寸／质量／张数生成报价。
2. 提交时校验报价，原子冻结金额，同时创建任务与待投递事件。
3. 保存并交付图片后结算；部分成功按成功张数收费。
4. 失败释放冻结金额；上游结果不明转为待核查，不盲目重发生图。

金额以整数分计算，上游成本用精确十进制单独记录。价格保留版本，已受理任务不因调价改变。无有效价格不销售。账本追加更正记录，不改写原流水。

充值预留微信／支付宝适配器。正式接入必须验签、核对订单、金额和交易号，重复通知仅入账一次，浏览器支付跳转不作为成功凭据。真实上线前完成查单、对账、漏通知恢复和原路退款。

生成失败释放与充值退款独立。首版整笔充值退款由管理员审核，申请时余额需足够并冻结，驳回释放、批准原路退回。未完成真实支付联调仅提供测试充值，不收真实资金。

## 5. 三天历史

- 提示词从任务创建起，参考图从上传起，结果从保存成功起，分别计算 72 小时。
- 到期立即拒绝页面、接口和下载访问；每小时物理清理，S3 生命周期兜底。
- 复用参考图不延长原文件寿命；用户可以提前删除。
- 未结算任务可保留必要状态，但不显示过期内容。
- 作品和提示词不进入长期备份；账号及必要财务元数据独立保留，图片删除不撤销消费。
- 到期明确提醒不可恢复，要求用户及时下载。

## 6. 技术方案

| 层级 | 选型 |
| --- | --- |
| 前端及 API | Next.js、React、TypeScript |
| 界面 | Tailwind CSS、Radix UI、Lucide |
| 参数校验 | Zod |
| 数据库 | PostgreSQL＋Prisma |
| 异步任务 | BullMQ＋Redis＋独立 Node.js Worker |
| 文件 | 私有 S3 兼容存储 |
| 认证 | Argon2id＋服务端 Session |
| 部署 | Docker Compose＋Nginx＋HTTPS |
| 测试 | Vitest、Playwright |

采用通用开源组件自建业务，不依赖 New API 或积分站源码。依赖通过 lockfile 固定。

浏览器 → API → 数据库事务冻结余额并记录任务／Outbox → 队列 → Worker → 上游 → 私有存储 → 结算 → 展示。网页关闭不影响任务。数据库为账务和任务事实来源，Redis 队列仅存任务 ID。

模型定义用户能力与价格，渠道定义上游协议和凭据引用。适配器统一提交、查询、解析，不假设中转接口兼容 OpenAI。视频预留媒体类型和任务结构，未来增加独立视频参数及计价，不在首版显示不可用入口。

## 7. 软硬件与部署

开发建议 4 核、16 GB；测试服务器 2 vCPU、4 GB；首期生产建议 4 vCPU、8 GB、80 GB SSD、Ubuntu 24.04 LTS。带宽建议 10 Mbps 起步，实际容量以压测为准。无需 GPU。

每日 1,000 张、每张 5 MB，三天结果约 15 GB，参考图和余量另算。生产文件使用独立私有对象存储。首期可单机部署，增长后先拆分 Worker 和数据库，不承诺单机高可用。

部署前准备域名、TLS、云服务器、私有存储、供应商凭据、支付商户和加密异地数据库备份。短信和邮件暂不采购。

## 8. 实施与验收

顺序：设计基础 → 账号与任务 → 真实生图适配 → 账本与支付 → 管理与清理 → 上线验证。

验收必须覆盖文／图生图、原生参数、跨设备、下载、并发余额、部分成功、重复通知、超时核查、用户隔离、到期拒绝、恢复码失效、手机流程和服务恢复。

监控任务积压、上游异常、长期冻结、支付漏到账和清理失败。固定镜像发布，迁移保持兼容，在独立环境演练备份恢复。缺少真实接口和商户资料时，只能完成本地测试，不构成商业上线。

详细交付状态见 README.md、docs/VALIDATION.md；部署见 docs/OPERATIONS.md；接口见 docs/API.md。
